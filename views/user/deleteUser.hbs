<div class="w-[93%] md:w-[50%] lg:w-[40%] bg-white shadow-lg mx-auto my-8 px-6 py-6 rounded-xl border border-gray-300">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">Benutzer löschen</h2>
    <p class="text-gray-700 mb-6">Möchten Sie den folgenden Benutzer wirklich löschen?</p>

    <div class="overflow-x-auto mb-6">
        <table class="w-full text-left border-collapse">
            <tbody>
                <tr class="border-b border-gray-300">
                    <td class="font-semibold py-2">Name:</td>
                    <td class="py-2">{{user.firstname}} {{user.lastname}}</td>
                </tr>
                <tr class="border-b border-gray-300">
                    <td class="font-semibold py-2">E-Mail:</td>
                    <td class="py-2">{{user.email}}</td>
                </tr>
                <tr>
                    <td class="font-semibold py-2">Adresse:</td>
                    <td class="py-2">{{user.street}} {{user.houseNumber}}, {{user.zip}} {{user.city}}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <form action="/user/usermanagement/delete" method="POST">
        <input type="hidden" name="userID" value="{{user.userID}}" />

        <div class="flex justify-end gap-3">
            <!-- Abbrechen Button -->
            <button type="button" onclick="window.history.back()" 
                    class="bg-gray-300 text-gray-800 font-medium px-6 py-2 rounded hover:bg-gray-400 transition-colors">
                Nein
            </button>

            <!-- Löschen Button -->
            <button type="submit" 
                    class="bg-red-600 text-white font-medium px-6 py-2 rounded hover:bg-red-700 transition-colors">
                Ja, löschen
            </button>
        </div>
    </form>
</div>
